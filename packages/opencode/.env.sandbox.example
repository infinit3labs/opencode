# Copy to .env.sandbox and update values before running docker compose.
OPENCODE_SERVER_PASSWORD=replace-with-long-random-secret
OPENCODE_SERVER_USERNAME=opencode
OPENCODE_PORT=4096

# Absolute path to the project directory the container should be allowed to access.
OPENCODE_PROJECT_DIR=/absolute/path/to/project
